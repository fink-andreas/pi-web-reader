/**
 * Pi Web Reader Extension
 *
 * Fetches and extracts readable text from web pages, returning raw Markdown.
 * Full implementation to be added in subsequent issues.
 */

import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function webReaderExtension(pi: ExtensionAPI) {
	// Tool registration will be implemented in INN-51
	// This stub ensures the TypeScript configuration works correctly
}
